# Project: [Project Name]

<!--
HOW TO USE THIS TEMPLATE:
1. Copy this file to your project: cp project-CLAUDE-template.md /path/to/project/.claude/CLAUDE.md
2. Fill in each section below
3. Keep it updated as the project evolves — Claude will read this at the start of every session
-->

## Context for AI Agents

[Brief description of what this project does, who uses it, and its current state.
Example: "A Node.js API server for managing survey templates. Used internally.
Currently in active development — auth is not yet implemented."]

## Active Work

[What is currently being worked on or what was last worked on.
Example: "Adding email notification support. The endpoint exists but sending is not wired up yet."]

## Architecture Notes

[Key files, patterns, or decisions an agent should know before touching code.
Example:
- Entry point: src/index.js
- Database: SQLite via better-sqlite3, schema in db/schema.sql
- All routes are in src/routes/, one file per resource
- Tests use Vitest, run with `npm test`]

## Preferences for This Project

[Any project-specific Claude behaviors or constraints.
Example:
- Don't modify the database schema without asking first
- Keep all logic in src/, don't create top-level files
- Run `npm test` after any change to src/]
